[MASTER]
# Specify additional import paths (comma-separated)
init-hook='import sys; sys.path.append("src")'

[MESSAGES CONTROL]
# Disable some pylint messages (comma-separated)
disable=missing-docstring, invalid-name

[FORMAT]
# Set the output format (text, parseable, json, etc.)
output-format=text
